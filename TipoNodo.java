package biblioteca;

/**
 * Tipo de nodo en el grafo bipartito de la biblioteca.
 * Solo existen nodos de usuario o de libro (conjuntos disjuntos).
 */
public enum TipoNodo {
    USUARIO,
    LIBRO
}
